# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          cd _bball_stats_
          npm install
      - name: Run linting
        run: |
          cd _bball_stats_
          npm run lint
      - name: Build
        run: |
          cd _bball_stats_
          npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BASKETBALL_STATS_TRACKER_18554 }}
          channelId: live
          projectId: basketball-stats-tracker-18554
