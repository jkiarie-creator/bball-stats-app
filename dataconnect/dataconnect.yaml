specVersion: "v1beta"
serviceId: "bball-stats-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bball-stats-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
